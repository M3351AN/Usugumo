[package]
name = "usugumo"
version = "3.0.0"
edition = "2024"
build = "build.rs"

[lib]
name = "usugumo_rs"
crate-type = ["cdylib"]

[build-dependencies]
cc = "1"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
